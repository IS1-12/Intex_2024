@{
    Layout = "_AdminLayout";
}
@model IQueryable<LegosWithAurora.Models.Customer>

<h1>All Products</h1>
<div class="align-content-center">
    @foreach (var p in Model.Take(3))
    {
        <partial name="Components/EditUser/Default" model="p" />
        <br />
    }
</div>